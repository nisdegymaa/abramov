#include "iostream"
#include "cmath"

using namespace std;

int main() {
    double a;
    cin >> a;

    if(a >= 0 && a <= 1.5);

    cout.setf(ios::fixed|ios::showpoint);
    cout.precision(1);

    return 0;
}       